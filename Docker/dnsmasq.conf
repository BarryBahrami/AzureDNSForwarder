# Disable all DHCP functionality
no-dhcp-interface=

# DON'T read local /etc/resolv.conf for upstream servers
no-resolv

# Forward all DNS requests to 168.63.129.16
server=168.63.129.16

# Disable logging
log-facility=-
